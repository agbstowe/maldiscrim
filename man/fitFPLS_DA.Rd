% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fplsda.R
\name{fitFPLS_DA}
\alias{fitFPLS_DA}
\title{Fit Functionnal Partiel Least Square and Discriminant Analysis (fPLS_DA) Model}
\usage{
fitFPLS_DA(
  data,
  method = c("bsplines", "wavelets"),
  nbasis = 1050,
  ncomp = NULL,
  argvals = 2001:22664,
  rangeval = c(1999, 22664),
  n_folds = 5,
  filter = "la14",
  boundary = "periodic",
  n.levels = NULL,
  level = 3,
  precomputed = FALSE
)
}
\arguments{
\item{data}{A numeric matrix of processed Mean Mass Spectra (MSP) where rows
represent biological replicates (with strain names as rownames) and columns represent spectral intensities.}

\item{method}{Character. The mathematical basis used for spectral decomposition: either \code{"bsplines"}
(B-spline basis functions) or \code{"wavelets"} (Discrete Wavelet Transform).}

\item{nbasis}{Integer. Number of basis functions used when \code{method = "bsplines"}. Default is \code{1050}.}

\item{ncomp}{Integer. Number of PLS components to retain. If \code{NULL} (default), the optimal number of
components is automatically determined using the one-sigma heuristic based on Cross-Validation RMSEP.}

\item{argvals}{A numeric vector specifying the evaluation grid (m/z indices) for the B-spline projection.}

\item{rangeval}{A numeric vector of length 2 defining the domain range for the B-spline basis.}

\item{filter}{Character. The wavelet filter type passed to \code{\link[wavelets]{dwt}} when \code{method = "wavelets"}. Default is \code{"la14"}.}

\item{boundary}{Character. The boundary handling method passed to \code{\link[wavelets]{dwt}}. Default is \code{"periodic"}.}

\item{level}{Integer. The specific wavelet decomposition resolution level from which
coefficients are extracted for downstream classification. Default is \code{3}.}

\item{precomputed}{Logical. If \code{TRUE}, the function bypasses all heavy computations and returns a built-in
pre-trained model object. Primarily used to speed up examples and unit testing. Default is \code{FALSE}.}

\item{nfolds}{Integer. Number of folds for the internal cross-validation performed during PLS fitting. Default is \code{5}.}

\item{nlevels}{Integer. Total number of decomposition levels for the wavelet transform.
If \code{NULL}, it is automatically set to the maximum possible level.}
}
\value{
An object of class \code{"FPLS_DA"}. This object is a named list containing:
\item{PLSmodel}{The fitted PLS model object returned by \code{pls::plsr}.}
\item{LDAmodel}{The fitted LDA model object returned by \code{MASS::lda}.}
\item{method}{A character string indicating the functional decomposition method used.}
\item{basis}{The B-spline basis object (if \code{method = "bsplines"}), otherwise \code{NULL}.}
\item{wavelets}{A list containing raw DWT outputs for each spectrum (if \code{method = "wavelets"}), otherwise \code{NULL}.}
\item{ncompOpt}{The optimised or user-defined number of PLS components retained.}
\item{const_idx}{A logical vector indicating which PLS components were excluded due to near-zero variance.}
\item{levels}{The structural names/categories of the groups (strains).}
\item{labels}{The original factor vector of group memberships assigned to each row.}
\item{argvals}{The evaluation grid used for B-spline projection, or \code{NULL}.}
\item{filter}{The wavelet filter name used, or \code{NULL}.}
\item{nlevels}{The effective wavelet decomposition level count, or \code{NULL}.}
\item{level}{The specific wavelet resolution level extracted, or \code{NULL}.}
\item{nbasis}{The total number of B-spline basis functions used, or \code{NULL}.}
\item{boundary}{The wavelet boundary method applied, or \code{NULL}.}
}
\description{
\code{fitFPLS_DA} performs a supervised classification of MALDI-TOF mass spectra by combining functional data analysis (FDA)
dimensionality reduction techniques with Partial Least Squares Regression (PLSR) and Linear Discriminant Analysis (LDA).
}
\details{
The pipeline executes three sequential phases to achieve classification of functional spectral data:
\enumerate{
\item \strong{Functional Decomposition:} Depending on \code{method}, the high-dimensional spectral matrix is projected into either:
\itemize{
\item \emph{B-Splines:} Continuous functional representations are built using a B-spline basis matrix via \code{fda::Data2fd}.
\item \emph{Wavelets:} Multi-resolution analysis is applied via the Discrete Wavelet Transform (\code{wavelets::dwt}). A built-in guardrail automatically adjusts \code{level} if the remaining coefficients are insufficient for modeling.
}
\item \strong{PLS Dimension Reduction:} A Kernel Partial Least Squares Regression (\code{pls::plsr}) is fitted using dummy-coded groups as responses.
\item \strong{Classification:} Low-variance components are filtered out, and a Linear Discriminant Analysis (\code{MASS::lda}) is fitted on the remaining PLS scores to generate the final decision boundaries.
}
}
\examples{
# Model loading using the precomputed cache
model_res <- fitFPLS_DA(precomputed = TRUE)
# print(model_res)

\donttest{
# Real execution example using the package built-in data 'spectra100'
data("spectra100", package = "maldiscrim")

# Fit with B-Splines functional basis
bspline_model <- fitFPLS_DA(
  data = spectra100, method = "bsplines", nbasis = 1050, n_folds = 5
  )

# Fit with Wavelets multi-resolution decomposition
# wavelet_model <- fitFPLS_DA(
# data = spectra100, method = "wavelets", level = 3, n_folds = 5)
}

}
\seealso{
\code{\link[pls]{plsr}}, \code{\link[MASS]{lda}}, \code{\link[fda]{Data2fd}}, \code{\link[wavelets]{dwt}}
}
