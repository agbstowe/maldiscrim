% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internals_functions.R
\name{.fpcaClustering}
\alias{.fpcaClustering}
\title{FPCA decomposition and clustering on spectral data}
\usage{
.fpcaClustering(data, k, n, clust_method = "kmeans")
}
\arguments{
\item{data}{A numeric matrix of spectral intensities (rows = samples,
columns = m/z values). Column names must be set to the m/z indices before
calling this function.}

\item{k}{Integer. Number of clusters. Must be >= 2.}

\item{n}{Integer. Total number of spectra to simulate. Used to compute
\code{n_per_cluster} proportionally to observed cluster sizes.}

\item{clust_method}{Character. One of \code{"kmeans"} (default), \code{"hclust"}, or
\code{"gmm"}. See Details.}
}
\value{
A named list with the following elements:
\item{res_fpca}{The fitted FPCA object returned by \code{\link[fdapace:FPCA]{fdapace::FPCA()}}.}
\item{scores}{Numeric matrix of FPCA scores (rows = samples).}
\item{cluster_ids}{Integer vector of cluster assignments for each sample.}
\item{n_per_cluster}{Integer vector of target simulation counts per cluster,
proportional to the observed cluster sizes. Adjusted so that the total
equals \code{n}.}
}
\description{
Transforms a raw spectral matrix into functional data via
\code{\link[fdapace:MakeFPCAInputs]{fdapace::MakeFPCAInputs()}} and \code{\link[fdapace:FPCA]{fdapace::FPCA()}}, then applies the chosen
clustering method on the first two functional principal component scores to
identify latent sub-structures.
}
\details{
The three supported clustering methods all operate on the first two FPCA
score dimensions:
\itemize{
\item \code{"kmeans"} — K-means via \code{\link[stats:kmeans]{stats::kmeans()}} with \code{nstart = 25} and
a fixed seed for reproducibility. Fast and effective when clusters are
roughly spherical and of similar size.
\item \code{"hclust"} — Agglomerative hierarchical clustering via
\code{\link[stats:hclust]{stats::hclust()}} with Ward's D2 linkage (\code{method = "ward.D2"}),
followed by \code{\link[stats:cutree]{stats::cutree()}} at \code{k} groups. Deterministic (no seed
needed) and robust to outliers.
\item \code{"gmm"} — Gaussian Mixture Model via \code{\link[mclust:Mclust]{mclust::Mclust()}} with \code{G = k} components. Allows elliptical clusters of varying shape and size,
which is more realistic for biological data. The covariance structure is
selected automatically by BIC among standard \code{mclust} model families.
}
}
\keyword{internal}
